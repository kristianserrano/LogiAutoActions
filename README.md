# LogiAutoActions

This project is proprietary and all rights are reserved.

See `COPYRIGHT.md` for the full copyright and licensing notice.

## Run As Web App

1. Install dependencies:
   - `npm install`
2. Start the server:
   - `npm start`
3. Open the app in a browser:
   - `http://127.0.0.1:3000`

For local development with auto-reload, use `npm run dev`.

## Deployment Reference

- Minimal-ops ephemeral web deployment plan: `docs/EPHEMERAL_WEB_DEPLOYMENT_PLAN.md`
