{
  "name": "logi-auto-actions",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Node.js web app for generating Logitech Actions SDK plugin assets.",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js --watch src --watch public server.js",
    "start:web": "npm run start",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "dependencies": {
    "adm-zip": "^0.5.17",
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "express": "^5.2.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
